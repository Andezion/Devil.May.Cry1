#include <stdio.h>

int main()
{
    int x;
    int y;
    printf("Enter numbers: ");
    int a = scanf("%i",&x); int b = scanf("%i",&y);
    if (a != 1)
    {
        printf("Incorrect input");
        return 1;
    }
    if (b != 1)
    {
        printf("Incorrect input");
        return 1;
    }
    else
    {
        printf("%i",x+y);
    }
    return 0;
}
