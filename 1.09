#include <stdio.h>

int main()
{
    double x = 5.0;
    int y = 2;
    int z = (int)x * y;
    int c = (int)x + y;
    int v = (int)x - y;
    printf("%i %i\n",(int)x,y);

    printf("%i\n",z);
    printf("%.2f\n",x/y);
    printf("%i\n",c);
    printf("%i",v);
    return 0;
}
